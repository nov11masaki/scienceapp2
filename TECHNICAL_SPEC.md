# ScienceBuddy 技術仕様書

## 📝 最新更新（2025年11月16日）

### ✅ 改善内容
- **まとめ機能の柔軟化**: 2回以上のラリーがあれば、有意な内容がなくてもまとめを生成する
- **音声入力の改善**: 余分なスペースを削除してキーワード検査を確実にする
- **UI改善**: 
  - 予想ページ：再度ボタンクリック時に警告ダイアログ表示
  - 考察ページ：入力がある場合に単元選択に戻る時に警告表示
- **バグ修正**: 
  - `reflectionResumptionInfo` 未定義エラーを修正
  - `goBackToUnitSelection` 関数名を修正
  - 表フォーマット統一

---

## 1. システム概要

ScienceBuddy は、小学4年生の理科学習（熱と温度単元）における AI 対話型学習支援システムです。
子どもが「変化」に着目し、経験から予想を立て、実験を経て考察をまとめる学習プロセスを AI が支援します。

---

## 2. ユースケース図

```
┌─────────────────────────────────────────────────────────────────┐
│                        ScienceBuddy System                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐              ┌──────────────┐                 │
│  │ 学習者(児童) │              │  教員    　     │              　　│
│  └──────────────┘              └──────────────┘                 │
│         │                              │                         │
│         │                              │                         │
│    ┌────┴────────────────┬────────────┴───┐                     │
│    │                     │                 │                     │
│    ▼                     ▼                 ▼                     │
│  ┌────────────┐    ┌─────────────┐   ┌──────────┐              │
│  │ クラス/    │    │ AI対話      │   │ 学習     │              │
│  │出席番号    │    │(予想/考察)  │   │ログ確認 │              │
│  │選択        │    │             │   │          │              │
│  └────────────┘    └─────────────┘   └──────────┘              │
│       │                   │                 │                   │
│       │        ┌──────────┴────────────┐   │                   │
│       │        │                       │   │                   │
│       ▼        ▼                       ▼   ▼                   │
│   ┌────────────────────────────────────────────┐               │
│   │      単元選択                               │               │
│   │  (金属/水/空気/冷却)                       │               │
│   └────────────────────────────────────────────┘               │
│             │                                                   │
│       ┌─────┴─────┐                                             │
│       │            │                                             │
│       ▼            ▼                                             │
│   ┌────────┐   ┌──────────┐                                     │
│   │ 予想   │──▶│ 実験     │                                    │
│   │段階    │   │(記録)    │                                    │
│   └────────┘   └──────────┘                                    │
│       │              │                                           │
│       │              │                                           │
│       └──────┬───────┘                                           │
│              │                                                   │
│              ▼                                                   │
│          ┌────────┐                                              │
│          │ 考察   │                                              │
│          │段階    │                                              │
│          └────────┘                                              │
│              │                                                   │
│              ▼                                                   │
│        ┌──────────┐                                              │
│        │ 学習完了 │                                              │
│        │ログ保存 │                                              │
│        └──────────┘                                              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

### アクター

| アクター      | 説明         | アクション |
|-------------|------------|----------|
| **学習者(児童)** | 小学4年生 | クラス・出席番号選択 → 単元選択 → 予想ステージ(AI対話) → 考察ステージ(AI対話) |
| **教員**       | 学級担任   | 学習ログ確認、生徒進捗管理、ノート写真確認 |
| **AI(OpenAI)** | gpt-4o-mini | 対話応答、要約生成、フィードバック提供 |

---

## 3. システムアーキテクチャ

### 3.1 全体構成図

```
┌──────────────────────────────────────────────────────────────────────┐
│                          ブラウザ (フロントエンド)                     │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  HTML/CSS/JavaScript (Bootstrap 5, Vanilla JS)                │  │
│  │  - prediction.html       (予想ステージ)                       │  │
│  │  - reflection.html       (考察ステージ)                       │  │
│  │  - select_unit.html      (単元選択)                           │  │
│  │  - teacher/dashboard.html (教員向けダッシュボード)            │  │
│  └────────────────────────────────────────────────────────────────┘  │
└────────────────────────┬───────────────────────────────────────────────┘
                         │ HTTP/REST
┌────────────────────────▼───────────────────────────────────────────────┐
│                   Flask (Python 3.12)  - バックエンド                   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  app.py (メインアプリケーション)                               │  │
│  │  ┌──────────────────────────────────────────────────────────┐ │  │
│  │  │ ルート (Routes)                                          │ │  │
│  │  │ - GET  /             (ホーム)                            │ │  │
│  │  │ - GET  /select_class (クラス選択)                        │ │  │
│  │  │ - GET  /select_number(出席番号選択)                      │ │  │
│  │  │ - GET  /select_unit  (単元選択)                          │ │  │
│  │  │ - GET  /prediction   (予想ステージ)                      │ │  │
│  │  │ - POST /chat         (AI対話)                            │ │  │
│  │  │ - POST /summary      (予想まとめ生成)                    │ │  │
│  │  │ - GET  /reflection   (考察ステージ)                      │ │  │
│  │  │ - POST /reflect_chat (考察対話)                          │ │  │
│  │  │ - POST /final_summary(考察まとめ生成)                    │ │  │
│  │  │ - GET  /teacher      (教員ログイン)                      │ │  │
│  │  └──────────────────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────────────────┐ │  │
│  │  │ ビジネスロジック                                          │ │  │
│  │  │ - get_ai_response()        (OpenAI API呼び出し)         │ │  │
│  │  │ - get_initial_ai_message() (初期メッセージ取得)         │ │  │
│  │  │ - validate_summary()       (要約の内容検証)             │ │  │
│  │  │ - save_learning_log()      (学習ログ保存)               │ │  │
│  │  │ - load_learning_logs()     (ログ読み込み)               │ │  │
│  │  │ - update_student_progress()（進捗更新）                │ │  │
│  │  └──────────────────────────────────────────────────────────┘ │  │
│  └────────────────────────────────────────────────────────────────┘  │
└────────────────────────┬───────────────────────────────────────────────┘
                         │ REST API Call
        ┌────────────────┴────────────────┐
        │                                 │
        ▼                                 ▼
┌──────────────────┐              ┌─────────────────────────┐
│  OpenAI API      │              │  ローカルストレージ      │
│  (gpt-4o-mini)   │              │                         │
└──────────────────┘              │  - session_storage.json │
                                  │  - learning_progress.json
                                  │  - logs/                │
                                  │    learning_log_*.json  │
                                  │  - prompts/             │
                                  │  - templates/           │
                                  │  - static/              │
                                  └─────────────────────────┘
```

### 3.2 セッション管理アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────┐
│                      セッション管理の流れ                        │
└─────────────────────────────────────────────────────────────────┘

【新規開始時 (resume=false)】
┌────────────────┐
│ GET /prediction│
│ resume=false   │
└────────┬───────┘
         │
         ▼
┌──────────────────────────┐
│ session.clear()          │
│ すべてのセッション初期化  │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ 新規会話セッション開始    │
│ - conversation: []       │
│ - summary: ''            │
│ - summary_created: False │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ 初期AIメッセージ追加     │
│ conversation: [{         │
│   role: 'assistant',     │
│   content: '...'         │
│ }]                       │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ learning_log に記録      │
│ (log_type: 'prediction') │
└──────────────────────────┘

【ページリロード時 (resume=true)】
┌────────────────┐
│ GET /prediction│
│ resume=true    │
└────────┬───────┘
         │
         ▼
┌──────────────────────────────────┐
│ セッションから conversation取得  │
│ (存在する場合は復帰)             │
└────────┬───────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ 要約が完了していた場合          │
│ 前回の要約を復帰                │
└────────┬───────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ 元の会話状態で再開               │
└──────────────────────────────────┘

【新規単元選択時】
┌──────────────────────┐
│ 異なる unit に移動   │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────────┐
│ 旧単元の会話をポップ     │
│ session.pop('unit'...)   │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ 新規セッション開始       │
│ (resume=false 扱い)      │
└──────────────────────────┘
```

---

## 4. データフロー

### 4.1 予想ステージのデータフロー

```
【初期化フェーズ】
┌─────────────────────────────────────────────────────────────┐
│ 1. ユーザーが「予想をしよう」をクリック                      │
│    GET /prediction?class=5&number=30&unit=空気の温度と体積  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────┐
        │ Flask: 単元情報とタスク読み込み│
        │ - prompts/ から初期メッセージ │
        │ - tasks/ から課題文読み込み    │
        └────────────┬───────────────────┘
                     │
                     ▼
        ┌────────────────────────────────┐
        │ HTML: prediction.html 表示     │
        │ - 課題文表示                   │
        │ - 初期AIメッセージ表示         │
        │ - 入力フィールド表示           │
        └────────┬───────────────────────┘
                 │
                 ▼
       ┌─────────────────────────┐
       │ DOM Ready              │
       │ JavaScript 初期化      │
       │ - イベントリスナー登録│
       │ - session状態確認      │
       └─────────────────────────┘

【対話フェーズ】
┌──────────────────────────────────────────────┐
│ 2. 児童が入力して「送信」をクリック             │
└────────────┬─────────────────────────────────┘
             │
             ▼
  ┌──────────────────────────┐
  │ JavaScript 入力検証      │
  │ - 空白チェック          │
  │ - 長さ確認              │
  └────────┬─────────────────┘
           │
           ▼
  ┌──────────────────────────────┐
  │ POST /chat                  │
  │ Body: {                      │
  │   "message": "入力テキスト"  │
  │ }                            │
  └────────┬─────────────────────┘
           │
           ▼
  ┌────────────────────────────────────┐
  │ Flask: get_ai_response()           │
  │ ① session['conversation'] を取得   │
  │ ② OpenAI API に送信               │
  │    - model: gpt-4o-mini            │
  │    - messages: [                   │
  │        {role: user, content: ...}  │
  │      ]                             │
  │    - system prompt from単元別     │
  │ ③ AI応答受け取り                  │
  │ ④ session['conversation'] 更新    │
  │ ⑤ learning_log に記録             │
  └────────┬───────────────────────────┘
           │
           ▼
  ┌────────────────────────────┐
  │ Response JSON:             │
  │ {                          │
  │   "response": "AI応答",     │
  │   "message_count": 4,      │
  │   "suggest_summary": false │
  │ }                          │
  └────────┬───────────────────┘
           │
           ▼
  ┌────────────────────────────┐
  │ JavaScript: 応答表示      │
  │ - AIメッセージを追加       │
  │ - チャット画面スクロール   │
  │ - 入力フィールドクリア     │
  └────────────────────────────┘

【まとめ フェーズ】
┌────────────────────────────────────┐
│ 3. 児童が「予想をまとめる」をクリック│
└────────┬───────────────────────────┘
         │
         ▼
  ┌────────────────────────────────┐
  │ JavaScript: 確認ダイアログ      │
  │ (既にまとめた場合)              │
  │ ↓                                │
  │ ページリロード (resume=false)   │
  │ または                           │
  │ POST /summary                 │
  └────────┬───────────────────────┘
           │
           ▼
  ┌──────────────────────────────────────┐
  │ Flask: /summary エンドポイント        │
  │ ① conversation からユーザー         │
  │    メッセージを抽出                 │
  │ ② validate_summary() で検証        │
  │    - ユーザーメッセージ数 > 0 ?   │
  │    - 内容長 >= 10文字 ?            │
  │    - 経験/理由キーワード含む?     │
  │ ③ 検証失敗 → 400エラー返却       │
  │ ④ 検証成功 → まとめ生成          │
  │    OpenAI API呼び出し            │
  │ ⑤ 結果をsessionとlogに保存       │
  └────────┬───────────────────────┘
           │
           ▼
  ┌────────────────────────────┐
  │ Response JSON:             │
  │ {                          │
  │   "summary": "まとめ文"    │
  │ }                          │
  │ または                     │
  │ {                          │
  │   "error": "エラー メッセージ"│
  │ }                          │
  └────────┬───────────────────┘
           │
           ▼
  ┌────────────────────────────┐
  │ JavaScript:                │
  │ - 成功: まとめを表示       │
  │   入力フィールド非表示     │
  │   まとめボタン非表示       │
  │ - 失敗: alert表示         │
  │   ユーザーに再入力促進     │
  └────────────────────────────┘

【最終化 フェーズ】
┌─────────────────────────────────┐
│ 4. 児童が「次へ」をクリック        │
│    (実験ステージへ移動)           │
└────────┬────────────────────────┘
         │
         ▼
  ┌────────────────────────┐
  │ session保存           │
  │ - conversation_history │
  │ - summary              │
  │ - summary_created flag │
  └────────┬─────────────────┘
           │
           ▼
  ┌────────────────────────┐
  │ 次のステージへ遷移      │
  │ GET /experiment        │
  └────────────────────────┘
```

### 4.2 データベース/ストレージ構成

```
【ローカル環境】

session_storage.json
├─ session_id: {...}
│  ├─ class_number: "5"
│  ├─ student_number: "30"
│  ├─ unit: "空気の温度と体積"
│  ├─ conversation: [
│  │  {
│  │    role: "assistant",
│  │    content: "初期メッセージ..."
│  │  },
│  │  {
│  │    role: "user",
│  │    content: "「予想ですが...」"
│  │  },
│  │  {
│  │    role: "assistant",
│  │    content: "「そうですね...」"
│  │  }
│  │]
│  ├─ prediction_summary: "まとめ..."
│  ├─ prediction_summary_created: true
│  └─ timestamp: "2025-11-16 22:55:00"
└─ ...

learning_progress.json
├─ student_key (class_number:student_number): {
│  ├─ class_number: "5"
│  ├─ student_number: "30"
│  ├─ units: {
│  │  │"空気の温度と体積": {
│  │  │  ├─ stage: "reflection"
│  │  │  ├─ prediction_summary_created: true
│  │  │  ├─ reflection_summary_created: false
│  │  │  ├─ last_access: "2025-11-16 22:55:00"
│  │  │  └─ conversation_history: [...]
│  │  └─}
│  └─}
│  └─ last_access: "2025-11-16 22:55:00"
└─}

logs/learning_log_20251116.json
├─ {
│  ├─ timestamp: "2025-11-16 22:45:30"
│  ├─ student_number: "30"
│  ├─ class_number: "5"
│  ├─ unit: "空気の温度と体積"
│  ├─ log_type: "prediction"
│  ├─ data: {
│  │  ├─ user_message: "予想としては..."
│  │  ├─ ai_response: "いい例ですね..."
│  │  └─ message_count: 1
│  │}
│  └─ stage: "prediction"
├─},
├─ {
│  ├─ timestamp: "2025-11-16 22:55:40"
│  ├─ student_number: "30"
│  ├─ log_type: "prediction_summary"
│  ├─ data: {
│  │  ├─ summary: "あたためると..."
│  │  └─ conversation: [...]
│  │}
├─},
└─ ...

prompts/
├─ initial_messages.json {
│  ├─ "空気の温度と体積": "こんにちは。..."
│  ├─ "金属のあたたまり方": "..."
│  └─ ...
│}
├─ 空気の温度と体積.md       (単元別system prompt)
├─ 金属のあたたまり方.md
├─ 水のあたたまり方.md
└─ 水を冷やし続けた時の温度と様子.md

【本番環境 (Google Cloud)】

Cloud Firestore
└─ students/{class}/{number}/units/{unit}
   ├─ conversation_history: [...]
   ├─ prediction_summary: "..."
   ├─ reflection_summary: "..."
   └─ timestamps: {...}

Google Cloud Storage (gs://bucket/)
├─ logs/
│  └─ learning_log_{date}.json
├─ uploads/
│  └─ note_photos/{class}/{number}/{photo_id}.jpg
└─ ...
```

---

## 5. データ流動図

```
          ┌─────────────────────────────┐
          │  児童 入力                   │
          │ (キーボード/音声)           │
          └──────────────┬──────────────┘
                         │
                         ▼
        ┌────────────────────────────────┐
        │ Input Validation (JS)          │
        │ - 空白チェック                 │
        │ - 長さチェック (最小1文字)    │
        └──────────────┬─────────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │ POST /chat                     │
        │ {                              │
        │   "message": "ユーザー入力"    │
        │ }                              │
        └──────────────┬─────────────────┘
                       │
                       ▼
        ┌────────────────────────────────────────┐
        │ Flask Backend                           │
        │                                         │
        │ 1. Session からconversation取得       │
        │ 2. ユーザーメッセージをconversationに │
        │    追加                                  │
        │ 3. OpenAI API呼び出し                 │
        │    - Model: gpt-4o-mini                │
        │    - Temperature: 0.7                  │
        │    - Max Tokens: 500                   │
        │    - System prompt: prompts/XXX.md    │
        │    - Messages: conversation            │
        │ 4. AI応答受け取り                      │
        │ 5. Session更新                         │
        │ 6. Learning Log保存                    │
        └──────────────┬────────────────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │ Response JSON                  │
        │ {                              │
        │   "response": "...",           │
        │   "message_count": 4,          │
        │   "suggest_summary": false     │
        │ }                              │
        └──────────────┬─────────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │ JavaScript: 応答処理          │
        │ - HTMLに応答を追加            │
        │ - チャット画面を更新          │
        │ - 入力フィールドをリセット    │
        └──────────────┬─────────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │ UI表示                         │
        │ - AI応答をチャット表示        │
        │ - 入力フィールド準備完了      │
        └────────────────────────────────┘


        ┌─────────────────────────────┐
        │  児童「予想をまとめる」ボタン │
        │  をクリック                  │
        └──────────────┬──────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │ JavaScript: ボタン状態確認    │
        │ - 既にまとめた?               │
        │   YES → confirm()表示       │
        │   NO  → /summary呼び出し     │
        └──────────────┬─────────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │ POST /summary                  │
        │ {}                             │
        └──────────────┬─────────────────┘
                       │
                       ▼
        ┌────────────────────────────────────────┐
        │ Flask: /summary エンドポイント         │
        │                                        │
        │ 1. Session からconversation取得      │
        │ 2. Validate Summary():                │
        │    a) ユーザーメッセージ存在?        │
        │    b) 最小文字数 >= 10?             │
        │    c) 経験/理由キーワード含む?      │
        │    → 検証失敗: 400 エラー返却       │
        │ 3. OpenAI API呼び出し:               │
        │    プロンプト: "conversationから      │
        │              要点を整理して要約する"│
        │ 4. 要約結果をSession とLog に保存   │
        └──────────────┬────────────────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │ Response                       │
        │ {                              │
        │   "summary": "まとめ文",       │
        │   "session_id": "xxx",         │
        │   "log_id": "yyy"              │
        │ }                              │
        │ または                         │
        │ {                              │
        │   "error": "...",              │
        │   "is_insufficient": true      │
        │ }                              │
        └──────────────┬─────────────────┘
                       │
                  ┌────┴────┐
                  │          │
            成功  ▼          ▼ 失敗
        ┌────────────────┐  ┌──────────┐
        │まとめ表示      │  │alert表示 │
        │ボタン非表示    │  │再入力促進│
        │↓              │  └──────────┘
        │Learning Log   │
        │に保存         │
        └────────────────┘
```

---

## 6. API エンドポイント仕様

### 6.1 チャット API

```
POST /chat
Content-Type: application/json

Request Body:
{
  "message": "ユーザーの入力テキスト"
}

Response (Success):
{
  "response": "AIの応答テキスト",
  "message_count": 4,  // 総メッセージ数
  "suggest_summary": false,  // まとめ推奨?
  "timestamp": "2025-11-16T22:55:00Z"
}

Response (Error):
{
  "error": "エラーメッセージ",
  "status": 400 or 500
}
```

### 6.2 まとめ生成 API

```
POST /summary
Content-Type: application/json

Request Body:
{}

Response (Success):
{
  "summary": "まとめられた文章",
  "conversation_length": 8,
  "tokens_used": 350
}

Response (Validation Error - 400):
{
  "error": "まだ何も話していないようです。あなたの予想や考えを教えてください。",
  "is_insufficient": true
}

Response (Server Error - 500):
{
  "error": "まとめ生成中にエラーが発生しました。"
}
```

### 6.3 セッション復帰 API

```
GET /prediction?class=5&number=30&unit=空気の温度と体積&resume=true

Query Parameters:
- class: クラス番号 (1-4 or 研究室)
- number: 出席番号 (1-30)
- unit: 単元名
- resume: セッション復帰フラグ (true/false)

Response:
HTML (prediction.html)
+ session data 埋め込み (JSON)
```

---

## 7. 音声入力機能の仕様

### 7.1 音声認識パイプライン

```
┌─────────────────────────────────────────────┐
│ 音声入力ボタン クリック                     │
└────────────────┬────────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────┐
    │ Web Speech API 初期化      │
    │ - language: 'ja-JP'        │
    │ - continuous: true         │
    │ - interimResults: true     │
    └────────────┬───────────────┘
                 │
                 ▼
    ┌────────────────────────────┐
    │ マイク接続 & 権限要求          │
    │ (ブラウザ prompt)               │
    └────────────┬───────────────────┘
                 │
            ┌────┴────┐
    許可    │          │  拒否
    ▼       ▼          ▼
┌────┐  ┌──────┐ ┌────────┐
│OK  │  │error │ │alert表示│
└─┬──┘  └──────┘ └────────┘
  │
  ▼
┌──────────────────────────────────┐
│ 音声認識開始                      │
│ イベント: onstart                │
│ - ステータス "音声を認識中..."   │
│ - ボタン変更 (停止ボタンに)       │
└─────────────┬────────────────────┘
              │
              ▼
    ┌─────────────────────────────┐
    │ 音声データ収集 & 認識       │
    │ (リアルタイム処理)           │
    │ イベント: onresult          │
    │ ┌──────────────────────────┐│
    │ │ 途中結果 (interim)       ││
    │ │ ステータス表示のみ        ││
    │ │ "🎤 認識中: ..."          ││
    │ └──────────────────────────┘│
    │ ┌──────────────────────────┐│
    │ │ 確定結果 (final)          ││
    │ │ 入力フィールドに追加      ││
    │ │ event.resultIndex で      ││
    │ │ 重複を防止                ││
    │ └──────────────────────────┘│
    └─────────────┬────────────────┘
                  │
                  ▼
        ┌──────────────────────┐
        │ 停止ボタン クリック   │
        │ または                │
        │ 音声無く 停止        │
        └────────┬─────────────┘
                 │
                 ▼
    ┌────────────────────────────┐
    │ 音声認識終了                │
    │ イベント: onend            │
    │ - ステータス 非表示        │
    │ - ボタン元に戻す            │
    └──────────────┬─────────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ 入力フィールド準備完了│
        │ (メッセージ送信可能)  │
        └──────────────────────┘
```

### 7.2 二重入力 防止ロジック

```
【問題】
音声認識の onresult イベントが複数回発火
→ 途中結果 + 確定結果が重複して追加される

【解決】
event.resultIndex を使用
→ 前回处理済みの結果をスキップ

┌─────────────────────────────┐
│ onresult Event              │
│                             │
│ for (i = event.resultIndex) │
│ {                           │
│   - 新しい結果のみ処理      │
│   - 前回の結果をスキップ    │
│ }                           │
│                             │
│ if (isFinal) {              │
│   input.value += transcript │  // 確定のみ追加
│ }                           │
│                             │
│ // 途中結果は表示のみ       │
│ if (interim) {              │
│   statusText = '🎤 ...'     │  // UI更新
│ }                           │
└─────────────────────────────┘
```

---

## 8. セキュリティ・エラーハンドリング

### 8.1 入力検証

```
【Chat API】
- メッセージ非空 ✓
- 最小文字数 >= 1 ✓
- 最大文字数 <= 5000 (予定)
- SQLインジェクション チェック (予定)
- XSS 対策 (HTML エスケープ)

【Summary API】
- ユーザーメッセージ存在 ✓
- 最小文字数 >= 10 ✓
- 経験/理由キーワード含む ✓
```

### 8.2 エラーハンドリング

```
【フロント側】
try {
  POST /chat
} catch(error) {
  if (error.message) {
    alert('⚠️ ' + error.message)  ← ユーザーに表示
  }
  button.disabled = false  ← ボタン再活性化
}

【バック側】
try {
  response = openai.chat.completions.create(...)
} except OpenAIError as e:
  return jsonify({'error': 'APIエラーが発生'}), 500
except Exception as e:
  log.error(e)
  return jsonify({'error': '予期しないエラー'}), 500
```

---

## 9. パフォーマンス最適化

```
【フロント】
- JavaScript minify (本番)
- CSS 最適化
- 画像 compress
- Lazy Loading (実装予定)

【バック】
- LRU Cache: get_initial_ai_message()
- Session compression (大会話用)
- Batch logging (複数メッセージ)
- Connection Pooling (本番)

【API】
- Rate limiting (本番・Google Cloud)
- Token制限: max_tokens=500
- Temperature: 0.7 (多様性と安定性のバランス)
```

---

## 10. デプロイメント

### ローカル開発
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
echo "OPENAI_API_KEY=sk-..." > .env
python app.py
# http://localhost:5014
```

### Google Cloud Run 本番
```bash
gcloud run deploy science-buddy \
  --source . \
  --platform managed \
  --region asia-northeast1 \
  --set-env-vars OPENAI_API_KEY=...,FLASK_ENV=production
```

---

## 11. 今後の拡張予定

- [ ] 音声出力 (TTS) 機能
- [ ] 教員向け可視化ダッシュボード改善
- [ ] 複数言語対応 (英語等)
- [ ] モバイルアプリ化
- [ ] リアルタイム協働学習 (複数生徒)
- [ ] 学習分析 AI (学習傾向分析)

